apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: invoice-analyzer
  annotations:
    alb.ingress.kubernetes.io/certificate-arn: <DEV_CERTIFICATE_ARN>
    external-dns.alpha.kubernetes.io/hostname: dev.oppsdev.xyz
    alb.ingress.kubernetes.io/security-groups: <DEV_SECURITY_GROUP_ID>
    alb.ingress.kubernetes.io/tags: Environment=dev,Application=invoice-analyzer
spec:
  rules:
  - host: dev.oppsdev.xyz
    http:
      paths:
      - path: /api
        pathType: Prefix
        backend:
          service:
            name: backend
            port:
              number: 8000
      - path: /
        pathType: Prefix
        backend:
          service:
            name: frontend
            port:
              number: 80
