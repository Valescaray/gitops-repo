apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: monitoring

bases:
  - namespace.yaml
  - ../../../base/crds
  - ../../../base/monitoring/prometheus-operator
  - ../../../base/monitoring/grafana
  - secretstore.yaml

commonLabels:
  environment: dev
