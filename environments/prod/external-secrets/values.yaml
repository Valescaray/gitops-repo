# External Secrets Operator - Prod Environment Values
# These values override the default Helm chart values for the prod environment

# Note: The main operator configuration is in the ArgoCD application
# This file is for any additional prod-specific overrides

# Example: If you need to add more service account annotations
# serviceAccount:
#   annotations:
#     additional-annotation: value
