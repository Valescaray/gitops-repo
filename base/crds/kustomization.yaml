apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# External Secrets CRDs are now managed by the Helm chart (installCRDs: true)
# Only Prometheus Operator CRDs are managed here

resources:
  - prometheusrule-crd.yaml
  - servicemonitor-crd.yaml
  - prometheus-crd.yaml
  - podmonitor-crd.yaml
  - alertmanager-crd.yaml
  - alertmanagerconfig-crd.yaml
  - thanosruler-crd.yaml
  - scrapeconfig-crd.yaml
  - prometheusagent-crd.yaml
  - probe-crd.yaml


commonLabels:
  app.kubernetes.io/managed-by: kustomize

