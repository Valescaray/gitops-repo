---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: externalsecrets.external-secrets.io
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
    controller-gen.kubebuilder.io/version: v0.13.0
  labels:
    app.kubernetes.io/name: external-secrets
spec:
  group: external-secrets.io
  names:
    categories:
    - externalsecrets
    kind: ExternalSecret
    listKind: ExternalSecretList
    plural: externalsecrets
    shortNames:
    - es
    singular: externalsecret
  scope: Namespaced
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: ExternalSecret is the Schema for the external-secrets API.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation of an object.'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this object represents.'
            type: string
          metadata:
            type: object
          spec:
            description: ExternalSecretSpec defines the desired state of ExternalSecret.
            properties:
              data:
                description: Data defines the connection between the Kubernetes Secret keys and the Provider data
                items:
                  description: ExternalSecretData defines the connection between the Kubernetes Secret key and the Provider data.
                  properties:
                    remoteRef:
                      description: ExternalSecretDataRemoteRef defines Provider data location.
                      properties:
                        conversionStrategy:
                          description: Used to define a conversion Strategy
                          type: string
                        key:
                          description: Key is the key used in the Provider.
                          type: string
                        property:
                          description: Used to select a specific property of the Provider value.
                          type: string
                        version:
                          description: Used to select a specific version of the Provider value, if supported.
                          type: string
                      required:
                      - key
                      type: object
                    secretKey:
                      description: SecretKey defines the key in which the controller stores the value.
                      type: string
                  required:
                  - remoteRef
                  - secretKey
                  type: object
                type: array
              dataFrom:
                description: DataFrom is used to fetch all properties from a specific Provider data
                items:
                  description: ExternalSecretDataFromRemoteRef defines Provider data location.
                  properties:
                    extract:
                      description: Used to extract multiple key/value pairs from one secret
                      properties:
                        conversionStrategy:
                          description: Used to define a conversion Strategy
                          type: string
                        key:
                          description: Key is the key used in the Provider.
                          type: string
                        property:
                          description: Used to select a specific property of the Provider value.
                          type: string
                        version:
                          description: Used to select a specific version of the Provider value, if supported.
                          type: string
                      required:
                      - key
                      type: object
                    find:
                      description: Used to find secrets based on tags or regular expressions
                      properties:
                        conversionStrategy:
                          description: Used to define a conversion Strategy
                          type: string
                        name:
                          description: Finds secrets based on the name.
                          properties:
                            regexp:
                              description: Finds secrets base
                              type: string
                          type: object
                        tags:
                          additionalProperties:
                            type: string
                          description: Find secrets based on tags.
                          type: object
                      type: object
                  type: object
                type: array
              refreshInterval:
                description: RefreshInterval is the amount of time before the values are read again from the SecretStore provider
                type: string
              secretStoreRef:
                description: SecretStoreRef defines which SecretStore to use when fetching the secret data.
                properties:
                  kind:
                    description: Kind of the SecretStore resource (SecretStore or ClusterSecretStore)
                    type: string
                  name:
                    description: Name of the SecretStore resource
                    type: string
                required:
                - name
                type: object
              target:
                description: ExternalSecretTarget defines the Kubernetes Secret to be created
                properties:
                  creationPolicy:
                    description: CreationPolicy defines rules on how to create the resulting Secret
                    type: string
                  deletionPolicy:
                    description: DeletionPolicy defines rules on how to delete the resulting Secret
                    type: string
                  immutable:
                    description: Immutable defines if the final secret will be immutable
                    type: boolean
                  name:
                    description: Name defines the name of the Secret resource to be managed
                    type: string
                  template:
                    description: Template defines a blueprint for the created Secret resource.
                    properties:
                      data:
                        additionalProperties:
                          type: string
                        type: object
                      engineVersion:
                        description: EngineVersion specifies the template engine version
                        type: string
                      metadata:
                        description: ExternalSecretTemplateMetadata defines metadata fields for the Secret blueprint.
                        properties:
                          annotations:
                            additionalProperties:
                              type: string
                            type: object
                          labels:
                            additionalProperties:
                              type: string
                            type: object
                        type: object
                      type:
                        description: Used to define template Secret type
                        type: string
                    type: object
                type: object
            required:
            - secretStoreRef
            type: object
          status:
            description: ExternalSecretStatus defines the observed state of ExternalSecret.
            properties:
              binding:
                description: Binding represents a servicebinding.io Provisioned Service reference to the secret
                properties:
                  name:
                    description: 'Name of the referent.'
                    type: string
                required:
                - name
                type: object
              conditions:
                items:
                  properties:
                    lastTransitionTime:
                      format: date-time
                      type: string
                    message:
                      type: string
                    reason:
                      type: string
                    status:
                      type: string
                    type:
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
              refreshTime:
                description: refreshTime is the time and date the external secret was fetched and the target secret updated
                format: date-time
                nullable: true
                type: string
              syncedResourceVersion:
                description: SyncedResourceVersion keeps track of the last synced version
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
