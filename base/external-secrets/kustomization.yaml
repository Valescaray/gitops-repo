apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# The External Secrets Operator deployment is managed by Helm chart
# This only contains the application-specific SecretStore configuration

resources:
  - secretstore.yaml

commonLabels:
  app: external-secrets

